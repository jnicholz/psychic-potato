
  <!-- List View EJS Template
    SEE THE /js/views/partials/example_output.html to see what your output should look like. Your LMS1 output might be slightly different, integrate the concepts into 
    your current implementation as well as you can.

    Watch the class video to finish this off, but try to do it yourself first!
    Note that you must learn how to do this as the next assignment you will do it yourself!
  -->
  

<table id='<%=view.list.id%>' class="<%=view.list.tableClasses%>">
    
  <!-- Output Table Headers -->

      <% view.columns.forEach( function(col, key){ %>
          
      <th class="thead sortable <%=view.list.thClasses%>" data-name="<%=col.name%>"> 
        <%=col.label%> 
          <i id="<%=col.name%>-asc" class="fa fa-arrow-up" aria-hidden="true" style="display:none" ></i>
          <i id="<%=col.name%>-desc" class="fa fa-arrow-down" aria-hidden="true" style="display:none"></i> 
        <% }) %>
      </th>
      <th class="thead <%=view.list.thClasses%>"> Actions </th>
    </tr>
    
    <% data.forEach(function(de){ %>
      <tr data-id = "<%=de.id%>" data-bs-toggle="popover" data-bs-placement="top">
        <% view.columns.forEach(function(col, key){ %>
          <td >
            <%=de[col.name]%>
          </td>
        <% }) %>
        
        <td>
          <span class="fas fa-edit text-primary" ></span>
          <span class="fas fa-trash text-danger" data-bs-toggle="modal" data-bs-target="#myModal"></span>
        </td>
      </tr>
    <% }) %> 
  </table>
    <!-- Output Table data rows
    This process uses a loop within a loop.  Iterate the 'data' item passed into the view renderer.
    To output the column TD elements, iterate the 'view.columns' (see view model list 'columns' array) and use the 'name' attribute to get the value from the data item

    Remember to output a 'data-id' attribute into each row so you can retrieve and utilize it later when you click on the delete and edit icons
    
  -->
    
 
 
